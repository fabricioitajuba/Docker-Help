DOCKER-COMPOSE 

IMPORTANTE
Hoje o Docker compose já vem junto com o Docker, não sendo necessário realizar o download em separado.

===================================
Instalando o Docker-compose

$ sudo apt install docker-compose

Verificando se o Compose foi instalado e sua versão:

# docker compose version

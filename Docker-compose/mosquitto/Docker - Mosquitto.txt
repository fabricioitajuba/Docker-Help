mkdir docker
cd docker
mkdir mqtt
cd mqtt

sudo nano docker-compose.yml
-----------------------
version: '2'

services:
##Mosquitto
  mqtt:
    container_name: mosquitto
    image: eclipse-mosquitto:2.0.11
    restart: always
    ports:
      - "1883:1883"
      - "9001:9001"
    volumes:
      - /home/pi/docker/mqtt/config:/mosquitto/config
      - /home/pi/docker/mqtt/data:/mosquitto/data
      - /home/pi/docker/mqtt/log:/mosquitto/log
volumes:
  config:
  data:
  log:
-----------------------

docker-compose up -d

cd config

sudo nano mosquitto.conf
-----------------------
persistence true
persistence_location /mosquitto/data/
log_dest file /mosquitto/log/mosquitto.log
-----------------------

docker restart "id do container"

docker exec -it "id do container"sh

/ # mosquitto_passwd -c /mosquitto/config/mosquitto.passwd "node de usuário"

Password: "crie uma senha"
Reenter password: "senha"

/ # exit

sudo nano mosquitto.conf

-- inserir as seguintes linhas:
password_file /mosquitto/config/mosquitto.passwd
allow_anonymous false

listener 1883
listener 9001
protocol websockets
-----------------------------

docker restart "id do container"


para testar, pode usar o mqttbox como extensão do chrome

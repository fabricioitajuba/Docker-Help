Chat utilizando o OpenSwoole

-Para criar os containers:
# docker compose up -d

- Para "derrubar" os containers, apagar os volumes e redes:
# docker compose down -v

-Para acessar a página do chat:
no navegador: localhost

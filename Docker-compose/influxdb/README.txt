- Banco de dados InfluxDB

-Para criar o container:
# docker compose up -d

- Para "derrubar" o container, apagar os volumes e redes:
# docker compose down -v

-Para acessar a página:
no navegador: localhost:8086
